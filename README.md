# Desafio Técnico - Softtek (BankMoreSystem)

Este repositório contém a implementação da API BankMoreSystem e a suíte de testes automatizados desenvolvida em PowerShell.

## 🚀 Tecnologias Utilizadas
* .NET Core / C#
* Entity Framework com SQLite
* Docker & Docker Compose
* Scripts de Automação (PowerShell)

## 📂 Estrutura do Projeto
* `/src`: Código fonte das APIs.
* `/database`: Arquivos de inicialização e bancos SQLite.
* `/tests`: Scripts `.ps1` para validação de fluxos (Saldo, Login, Transferência).

## 🛠️ Como Executar os Testes
Para validar a solução, execute o script principal via PowerShell:
1. Acesse a pasta `tests`
2. Execute: `./executar_testes.ps1`
